name: Deploy Staging

on:
  push:
    branches: [staging]

# Netlify auto-deploys from the staging branch.
# This workflow is a placeholder — no action needed here.
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Staging deploy handled by Netlify
        run: echo "Netlify will auto-deploy from the staging branch."
